export class Question {
  _id: string;
  title: string;
  author: string;
  pages: number;
  year: number;
  publisher: string;

  constructor() {
    // this.id = Math.round(Math.random() * 1000);
  }
}
